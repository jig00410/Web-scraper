# LLM Processing Layer

This folder manages AI-based data processing.

It is used to:
- Send cleaned HTML content to the LLM
- Extract meaningful structured data
- Format the response properly

This layer is used when intelligent or selective data extraction is required.