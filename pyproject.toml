[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "web-scrape"
version = "0.1.0"
description = "Web scraping pipeline using Playwright"

[tool.setuptools.packages.find]
where = ["."]